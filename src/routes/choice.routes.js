import { Router } from 'express'
//import { choice } from '../controllers/choice.controller.js'
//import { choice } from '../middlewares/choice.middleware.js'

const routerChoice = Router()


//routerChoice.post("/choice", userSchemaValidation, signUp) 
//routerChoice.get("/poll/:id/choice", userSchemaValidation, signUp)

export default routerChoice